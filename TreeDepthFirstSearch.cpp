#include <bits/stdc++.h>
using namespace std;
// Definition for a binary tree node
// template <class T>
template <typename T>
class TreeNode
{
public:
    T data;
    TreeNode<T> *left;
    TreeNode<T> *right;
    TreeNode(const T data) : data(data), left(nullptr), right(nullptr) {}
};
int main()
{
    cout << string(35, '-') << endl;
    cout << string(35, '-') << endl;
}